/**
 * Grundriss Tool – Override-Konfiguration
 * =====================================================================
 * @module config
 * @description Überschreibt Standard-Konstanten, z. B. für API-Keys.
 *
 * Kopieren Sie diese Datei als 'config.local.js' und passen Sie die
 * Werte an. Die Datei 'config.local.js' wird von .gitignore ignoriert.
 *
 * Beispiel für config.local.js:
 *
 *   window._GR_CONFIG = {
 *     SUPABASE_URL: 'https://ihre-projekt-url.supabase.co',
 *     SUPABASE_ANON_KEY: 'ihr-anon-key'
 *   };
 */

// Leerer Platzhalter – Standardwerte aus constants.js werden verwendet.
window._GR_CONFIG = window._GR_CONFIG || {};